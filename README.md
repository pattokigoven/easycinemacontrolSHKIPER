# Barco ICMP Control Application

Приложение для управления воспроизведением фильмов на Barco ICMP через протокол Text over IP.

## 📦 Две версии приложения

### 🖥️ Desktop версия (barco_control.py)
Классическое GUI приложение на tkinter для локального использования.

### 🌐 Web версия (barco_web.py)
Современный веб-интерфейс для удаленного управления через браузер.
- ✅ Работает на любом устройстве (ПК, планшет, смартфон)
- ✅ Можно развернуть на Linux сервере
- ✅ Многопользовательский режим
- ✅ Real-time логирование через WebSocket
- ✅ REST API для интеграции

## Возможности

- ▶️ **Play** - воспроизведение фильма
- ⏸️ **Pause** - пауза воспроизведения
- ⏹️ **Stop** - остановка воспроизведения
- 🗑️ **Clear** - очистка плейлиста
- 💡 **Lamp ON** - включение лампы проектора
- 🌑 **Lamp OFF** - выключение лампы проектора

## Требования

### Desktop версия
- Python 3.6+
- tkinter (обычно входит в стандартную установку Python)

### Web версия
- Python 3.6+
- Flask, Flask-SocketIO, eventlet (см. requirements.txt)

## Установка

### Установка зависимостей

**Для Desktop версии:**
```bash
# tkinter обычно уже установлен
python --version
```

**Для Web версии:**
```bash
pip install -r requirements.txt
```

## Запуск

### Desktop версия

**Windows:**
```bash
# Способ 1 (рекомендуется):
run_barco_control.bat

# Способ 2:
python barco_control.py
```

**Linux/Mac:**
```bash
python barco_control.py
```

### Web версия

**Windows (для тестирования):**
```bash
python barco_web.py
```
Откройте браузер: http://localhost:5000

**Linux (продакшн):**
```bash
# Автоматическая установка
chmod +x install.sh
sudo ./install.sh
```
См. подробную инструкцию в [WEBUI_QUICKSTART.md](WEBUI_QUICKSTART.md)

**Docker:**
```bash
docker-compose up -d
```
Откройте браузер: http://ваш-сервер:5000

## Использование

1. **Подключение к устройству:**
   - Введите IP-адрес Barco ICMP (по умолчанию: 192.168.1.100)
   - Введите порт (по умолчанию: 9090)
   - Нажмите кнопку "Подключить"
   - Индикатор статуса станет зеленым при успешном подключении

2. **Управление воспроизведением:**
   - После успешного подключения все кнопки управления станут активными
   - Нажимайте соответствующие кнопки для управления воспроизведением

3. **Лог операций:**
   - Все команды и ответы отображаются в окне лога внизу приложения
   - Лог помогает отслеживать взаимодействие с устройством

## Настройка

### Изменение IP-адреса и порта по умолчанию

Откройте файл `barco_control.py` и измените значения в строках:

```python
self.ip_entry.insert(0, "192.168.1.100")  # Ваш IP-адрес
self.port_entry.insert(0, "43748")        # Официальный порт Barco ICMP
```

### Протокол команд

Приложение использует официальный протокол Barco ICMP Automation over IP (версия 0.8):

**Команды управления плеером (PLAYER):**
- `PLAYER.Play` - воспроизведение
- `PLAYER.Pause` - пауза
- `PLAYER.Stop` - остановка
- `PLAYER.Clear` - очистка загруженного контента

**Команды управления проектором (PROJECTOR):**
- `PROJECTOR.Turn Lamp On` - включение лампы
- `PROJECTOR.Turn Lamp Off` - выключение лампы

**Дополнительные возможности:**
- Автоматическое включение режима подтверждения команд (ACK)
- Поддержка нескольких команд в одной строке
- Точка с запятой (;) в конце каждой команды

### Порт подключения

Barco ICMP automation server прослушивает порт **43748**. Этот порт доступен через:
- Сетевой интерфейс проектора
- Один из двух LAN портов Barco ICMP

## Устранение неполадок

### Ошибка подключения

- Убедитесь, что Barco ICMP включен и подключен к сети
- Проверьте правильность IP-адреса и порта (порт должен быть 43748)
- Убедитесь, что нет файрвола, блокирующего соединение
- Порт доступен через сетевой интерфейс проектора или LAN порты ICMP

### Команды не работают

- Убедитесь, что используются правильные команды протокола
- Проверьте документацию вашего конкретного устройства Barco
- Команды могут отличаться в зависимости от версии прошивки

## Дополнительная информация

Подробности о протоколе Text over IP для Barco ICMP смотрите в документе:
`Barco ICMP - Automation over IP Protocol.0.8.pdf`

## Лицензия

MIT License
